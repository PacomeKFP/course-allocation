# Cœur (obligatoire pour le pipeline)
pandas>=2.0
numpy>=1.24

# Solveur MIP CP-SAT (src/solvers/mip_solver.py)
ortools>=9.10

# App interactive + dépendances transitives qui peuvent manquer
# en install --user selon la config
streamlit>=1.36
altair>=5.0
attrs>=22.0
jinja2>=3.1
idna>=3.0

# Export Excel multi-onglets
openpyxl>=3.1
